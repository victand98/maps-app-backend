import { RoleAttrs } from "../../models";
import { Roles } from "../../types";

export const roleData: RoleAttrs[] = [
  {
    name: Roles.admin,
    status: true,
  },
  {
    name: Roles.cyclist,
    status: true,
  },
];
